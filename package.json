{
  "name": "relationflow-proxy",
  "version": "1.0.0",
  "description": "OpenAI-compatible proxy for RelationFlow with Supabase Auth token lifecycle",
  "main": "dist/proxy.js",
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test test/proxy.test.ts",
    "start": "node --experimental-strip-types src/proxy.ts"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.5.0"
  }
}
